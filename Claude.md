# CLAUDE.md

## Security Rules

- `.env`로 시작하는 파일은 절대 읽거나 접근하거나 참조하지 않는다
- 다음과 같은 민감 정보는 절대 노출하지 않는다:
  - API keys
  - Database credentials
  - Access tokens
  - Private URLs 또는 내부 엔드포인트

---

## Project Context

이 프로젝트는 현재 소규모 사용자를 대상으로 하는 개인 프로젝트이다.

향후 확장 가능성을 고려하되, **현재 규모에 맞는 복잡도만 유지한다.**

과도한 설계(Over-engineering)는 지양한다.

항상 다음을 기준으로 판단한다:

> "지금 이 변경이 정말 필요한가?"

---

## Decision Principles

### "실제 문제"의 기준

다음 중 하나 이상에 해당할 때만 변경을 제안한다:

- 성능 저하가 측정 가능한 경우 (예: 느린 쿼리, 응답 시간 증가)
- 코드 중복으로 인해 유지보수가 어려운 경우
- 복잡도가 증가하여 가독성이 떨어지거나 버그 가능성이 커진 경우
- 현재 구조로 인해 기능 확장이 어려운 경우

### 핵심 원칙

- 실제 문제가 없다면 변경을 제안하지 않는다
- 미래를 가정한 불필요한 복잡도 추가를 금지한다
- 복잡한 추상화보다 단순하고 읽기 쉬운 코드를 우선한다
- 성급한 최적화(Premature Optimization)를 지양한다
- 항상 변경 비용 대비 효과를 고려한다

---

## Refactoring Guidelines

### 제안 형식

**AS-IS**  
현재 코드 구조와 방식을 설명한다

**TO-BE**  
개선된 구조와 방식을 설명한다

**Why It Matters**  
현재 문제 또는 향후 발생할 수 있는 문제를 설명한다

**Trade-offs**
- Pros: 얻는 이점
- Cons: 추가되는 복잡도나 비용
- Industry Practice: 실무에서 일반적으로 사용하는 방식과 그 이유
- When to apply: 지금 필요한지, 규모가 커졌을 때 필요한지

**Example Code**  
코드 구조 변경이 있는 경우 최소한의 before/after 코드를 포함한다  
(문서 수정이나 네이밍 변경은 제외)

---

## Query Guidelines

과도하게 복잡한 쿼리 메서드는 지양한다.

- 단순 조회: Spring Data JPA method naming 사용
- 메서드명이 길어지고 가독성이 떨어질 경우: JPQL (`@Query`) 사용
- 다음 조건에 해당할 경우에만 QueryDSL 사용:
  - 동적 조건이 많아지는 경우 (예: 3개 이상의 optional 조건)
  - 가독성이 명확하게 떨어지는 경우
  - 성능 최적화가 필요한 경우

---

## Performance Guidelines

성능 문제는 반드시 아래 중 하나로 검증되어야 한다:

- Slow query 로그
- API 응답 시간 증가
- DB 부하 증가
- 과도한 쿼리 발생 (예: N+1 문제)

근거 없는 성능 최적화는 제안하지 않는다.

---

## Documentation Rules

### README 필수 구성

README에는 반드시 다음을 포함한다:

- 프로젝트 개요
- 기술 스택
- 아키텍처 개요 (핵심 설계 결정 포함)
- 로컬 실행 방법
- 주요 기능
- 최근 주요 변경 사항

---

### README 수정 시점

다음 경우 README를 수정한다:

- 기술 스택 변경
- 아키텍처 구조 변경
- 실행 방법 변경
- 주요 기능 추가 또는 제거

---

### 수정 전 검증

README 수정 전 반드시 확인한다:

1. 기술 스택이 실제 코드와 일치하는지
2. 아키텍처 설명이 실제 구조와 일치하는지
3. 실행 방법이 현재 환경과 맞는지
4. 더 이상 유효하지 않은 내용이 없는지

---

### 문서 작성 원칙

- 구현되지 않은 기능은 문서화하지 않는다
- 문서는 실제 코드 기준으로 작성한다 (추측 금지)

---

## API Documentation Rules

다음을 포함해야 한다:

- Endpoint URL 및 HTTP Method
- Request 파라미터 및 Body 형식
- Response 형식 및 상태 코드
- 주요 에러 케이스

---

## Design Decision Documents

**Problem**  
어떤 문제를 해결하려는가?

**Solution**  
어떤 방식을 선택했는가?

**Alternatives**  
어떤 대안을 고려했고 왜 선택하지 않았는가?

**Trade-offs**  
무엇을 얻고 무엇을 잃었는가?

**Result**  
결과는 어땠는가?

## Test Code Guidelines

모든 코드에 대해 테스트 코드를 작성하지 않는다.

다음과 같은 경우에만 테스트 코드를 작성한다:

- 인증 / 인가 등 보안 관련 로직
- 비즈니스 로직이 복잡한 경우
- 엣지 케이스 발생 가능성이 있는 경우
- 도메인에서 중요한 핵심 기능

다음과 같은 경우 테스트 코드를 생략할 수 있다:

- 단순 CRUD 로직
- DTO 변환
- 명확하고 단순한 로직

테스트 코드 또한 유지보수 대상이므로,
실제 가치가 있는 경우에만 작성한다.